#include <stdio.h>;
#include <ctype.h>;
#include <string.h>;
#define MAXLEN 1000000 char input[MAXLEN];
int main () {
    scanf ("%s", input);
    int n = strlen(input);
    int count = 0;
        for (int i = i i <n -1; i++){
            if (input[il = '(' && inputli + 1] == ' ('){
        for(int j=i + 2; j< n - 1; j++)
            if (input[j] == ')' && input[j + 1] == ') 'K
            count++;
        ｝
    printf("0\n");
    return 0;
｝
